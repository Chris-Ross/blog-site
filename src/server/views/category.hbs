<main>
    <section>
        <div class="hero-image">
            <h1 class="CAT-s-h1">{{category.category}}</h1>
        </div>
        <section class="CAT-s-grid">
            {{#each category.posts}}
            <ul><a href="/posts/{{id}}">
                    <li>Title : {{title}}</li>
                    <img src="{{imageUrl}}" alt="Blog Picture" class="CAT-s-img">
                </a>
                <li>Body: {{body}}</li>
            </ul>
            {{/each}}
        </section>
    </section>
</main>